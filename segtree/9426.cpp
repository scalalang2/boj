#include<bits/stdc++.h>

using namespace std;

using ll = long long;
using ld = long double;
using pi = pair<int, int>;
using pll = pair<ll, ll>;

const int INF = 1e9 + 5;
const ll llINF = 1e18;

// N개의 쿼리가 들어오는 상황에서 중앙값을 찾아내는 세그먼트 트리라..?
// N이 연속적으로 들어올 때, 마지막 K개의 최고값은 어떻게 하는거지..?
// index를 가지고 있다가 해당 index의 값을 새걸로 교체하는 거지
// 그럼 결국 update / query 문에서 중앙값을 어떻게 상단으로 올릴지가 문제네
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    return 0;
}